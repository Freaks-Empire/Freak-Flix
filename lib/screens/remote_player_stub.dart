/// lib/screens/remote_player_stub.dart
/// Stub file for conditional imports - won't be used at runtime.

import 'package:flutter/material.dart';

Widget buildEmbedPlayer(String embedUrl) {
  return const Center(
    child: Text('Platform not supported', style: TextStyle(color: Colors.white)),
  );
}
