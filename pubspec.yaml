name: freak_flix
description: "Freak-Flix: local-only Netflix-style media library"
publish_to: "none"
version: 1.1.0+0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  provider: ^6.1.2         # state management
  file_picker: ^8.1.2      # folder/file picker (desktop + mobile)
  path: ^1.9.0             # path helpers
  http: ^1.2.2             # Trakt + AniList calls
  shared_preferences: ^2.3.3 # local persistence & caching
  microsoft_graph_api: ^0.0.14
  collection: ^1.18.0
  media_kit: ^1.1.10
  media_kit_video: ^1.2.4
  media_kit_libs_windows_video: ^1.0.0
  flutter_dotenv: ^5.1.0
  url_launcher: ^6.3.0
  cached_network_image: ^3.4.1

  crypto: ^3.0.7
  youtube_explode_dart: ^2.2.1
  media_kit_libs_android_video: ^1.3.8
  archive: ^4.0.7
  wakelock_plus: ^1.4.0
  flutter_foreground_task: ^9.1.0
  flutter_local_notifications: ^19.5.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  package_info_plus: ^9.0.0
  go_router: ^14.2.0
  uuid: ^4.5.2
  newrelic_mobile: 1.0.0
  lucide_icons: ^0.257.0
  youtube_player_iframe: ^5.1.0
  firebase_core: ^4.3.0
  firebase_analytics: ^12.1.0
  firebase_crashlytics: ^5.0.6
  firebase_performance: ^0.11.1
  cloud_firestore: ^6.1.1
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# MSIX packaging
  msix: ^3.16.7

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/

msix_config:
  display_name: Freak-Flix [13c733a]
  identity_name: FreaksEmpire.FreakFlix
  publisher: CN=MNDL
  msix_version: 1.0.366.0
  certificate_path: C:/certs/freakflix-dev.pfx
  certificate_password: P@ssw0rd!
  store: false