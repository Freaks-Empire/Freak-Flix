---
phase: 03-onboarding-and-source-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/onboarding_source_connection_service.dart
  - lib/widgets/onboarding/source_connection_cards.dart
  - lib/widgets/device_code_dialog.dart
  - lib/widgets/settings/remote_connection_dialog.dart
  - test/onboarding/source_connection_recovery_test.dart
autonomous: true
requirements:
  - ONB-02
  - ONB-03
  - ONB-04
user_setup:
  - service: microsoft-entra
    why: "Validate OneDrive device-code onboarding flow"
    env_vars:
      - name: GRAPH_CLIENT_ID
        source: "Microsoft Entra app registration -> Application (client) ID"
      - name: GRAPH_TENANT_ID
        source: "Microsoft Entra app registration -> Directory (tenant) ID"
    dashboard_config:
      - task: "Enable public client flows for device code"
        location: "Microsoft Entra -> App registrations -> Authentication"
must_haves:
  truths:
    - User can start local source setup from onboarding without being forced to complete it.
    - User can start OneDrive source setup and, on timeout/cancel, return to source list as incomplete.
    - User can choose SFTP, FTP, and WebDAV as separate protocol cards in onboarding.
    - User sees inline connection errors near relevant fields and a guided troubleshooting path before retry.
    - User sees a short checklist and docs link after repeated connection failures.
  artifacts:
    - lib/widgets/onboarding/source_connection_cards.dart renders local, OneDrive, SFTP, FTP, and WebDAV cards without default priority.
    - lib/services/onboarding_source_connection_service.dart maps connector outcomes to onboarding source statuses.
    - lib/widgets/device_code_dialog.dart returns explicit timeout/cancel outcome usable by onboarding status updates.
    - lib/widgets/settings/remote_connection_dialog.dart shows field-adjacent failures and escalated troubleshooting help.
    - test/onboarding/source_connection_recovery_test.dart verifies timeout, cancel, and repeated-failure guidance behavior.
  key_links:
    - onboarding source cards trigger connection service actions and update per-source checklist status.
    - OneDrive device-code dialog terminal states map to `incomplete` instead of success.
    - repeated remote/OneDrive failures trigger troubleshooting checklist plus docs link.
---

<objective>
Implement onboarding source-connection building blocks for local, OneDrive, and remote protocols with explicit recovery behavior.

Purpose: Deliver ONB-02/03/04 connector UX semantics in onboarding while preserving existing service adapters.
Output: Source connection orchestration service, protocol-card widgets, and connector recovery test coverage.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-source-setup/03-CONTEXT.md
@.planning/phases/03-onboarding-and-source-setup/03-RESEARCH.md
@lib/providers/library_provider.dart
@lib/services/graph_auth_service.dart
@lib/widgets/device_code_dialog.dart
@lib/widgets/settings/remote_connection_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build onboarding source-connection orchestration service and status mapping</name>
  <files>lib/services/onboarding_source_connection_service.dart</files>
  <action>Create an onboarding source service that wraps existing connectors (`LibraryProvider.pickAndScan`, `GraphAuthService`, and remote account dialog outcomes) and returns uniform status events for checklist UI. Encode locked behavior exactly: source setup is optional during onboarding, no implicit source-type priority, and OneDrive timeout/abandon states are returned as `incomplete` with no hard completion gate. Keep connector logic in existing services; this layer should orchestrate, not re-implement protocol clients.</action>
  <verify>flutter analyze lib/services/onboarding_source_connection_service.dart</verify>
  <done>Service produces deterministic status events for local, OneDrive, and remote setup attempts and distinguishes `connected`, `incomplete`, and `failed` outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement onboarding protocol cards and recovery UX behavior</name>
  <files>lib/widgets/onboarding/source_connection_cards.dart, lib/widgets/device_code_dialog.dart, lib/widgets/settings/remote_connection_dialog.dart</files>
  <action>Add onboarding source cards with separate entries for SFTP, FTP, and WebDAV (not grouped). Present Local and OneDrive cards in the same neutral list without default/preferred highlighting. Update device-code and remote-connection dialog behavior to keep failures inline near the relevant fields/steps, show guided troubleshooting before retry, and escalate to a short checklist plus docs link after repeated failures. Ensure OneDrive cancel/timeout returns user to source list with incomplete status (not success and not dead-end).</action>
  <verify>flutter analyze lib/widgets/onboarding/source_connection_cards.dart lib/widgets/device_code_dialog.dart lib/widgets/settings/remote_connection_dialog.dart</verify>
  <done>Onboarding source UI displays separate protocol cards and exposes inline failure + guided troubleshooting flows, with repeated-failure escalation and proper OneDrive incomplete return semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add connector recovery tests for local/OneDrive/remote onboarding paths</name>
  <files>test/onboarding/source_connection_recovery_test.dart</files>
  <action>Add tests validating source status mapping and recovery rules: local source add path succeeds when picker returns folder; OneDrive timeout/cancel maps to incomplete and returns to source list state; remote validation/auth failures stay inline and trigger troubleshooting escalation after repeated retries. Include assertions that onboarding completion availability is unaffected by source incompletion.</action>
  <verify>flutter test test/onboarding/source_connection_recovery_test.dart</verify>
  <done>Tests pass and prove ONB-02/03/04 onboarding source paths and recovery behaviors match locked decisions.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on source orchestration/widgets and `flutter test test/onboarding/source_connection_recovery_test.dart`.
</verification>

<success_criteria>
All source-type onboarding cards are available and independent, OneDrive interruption handling is explicitly incomplete, and retry guidance escalates predictably after repeated failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-source-setup/03-02-SUMMARY.md`
</output>
