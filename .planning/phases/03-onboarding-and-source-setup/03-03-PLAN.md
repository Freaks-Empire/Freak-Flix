---
phase: 03-onboarding-and-source-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - lib/main.dart
  - lib/app.dart
  - lib/router.dart
  - lib/screens/setup_screen.dart
  - test/onboarding/setup_wizard_flow_test.dart
  - test/router/onboarding_redirect_test.dart
autonomous: true
requirements:
  - ONB-01
  - ONB-02
  - ONB-03
  - ONB-04
must_haves:
  truths:
    - User moves through onboarding in a linear wizard with checklist-style progress states.
    - User is prompted to resume or restart when draft onboarding exists.
    - User can finish onboarding from a required final review even if no source was connected.
    - User can still complete setup with local, OneDrive, or remote source paths available.
    - After onboarding, browse entry defaults to all enabled sources without source-type priority.
  artifacts:
    - lib/screens/setup_screen.dart implements wizard steps for type cards, source setup, and required review summary.
    - lib/router.dart routes setup using onboarding state while preserving adult route protections.
    - lib/main.dart wires onboarding provider into app startup and provider graph.
    - test/onboarding/setup_wizard_flow_test.dart verifies wizard progression, resume/restart prompt, and review gate.
    - test/router/onboarding_redirect_test.dart verifies setup redirect behavior does not require source completion.
  key_links:
    - setup screen consumes onboarding provider and source-connection service to render live checklist status.
    - router redirect checks setup completion while ignoring source completion as a hard gate.
    - finish action in review step marks setup complete only after review acknowledgement.
---

<objective>
Integrate the full onboarding wizard into runtime routing and first-run UX with final review enforcement.

Purpose: Deliver end-to-end Phase 3 behavior by wiring state machine and source cards into setup flow.
Output: Updated setup screen/router/provider wiring plus onboarding flow and redirect regressions.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-source-setup/03-CONTEXT.md
@.planning/phases/03-onboarding-and-source-setup/03-RESEARCH.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-01-SUMMARY.md
@lib/main.dart
@lib/router.dart
@lib/screens/setup_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace setup screen with linear onboarding wizard and required review step</name>
  <files>lib/screens/setup_screen.dart</files>
  <action>Refactor setup into a linear wizard that uses checklist-style progress indicators and explicit step ordering: welcome/resume decision, library type cards (none preselected), adult privacy section, optional source setup cards (local, OneDrive, SFTP, FTP, WebDAV), and required final review summary. Add resume-vs-restart prompt when draft state exists. Enforce final review acknowledgement before finish, but allow finish when sources remain incomplete (locked optional-source decision).</action>
  <verify>flutter analyze lib/screens/setup_screen.dart</verify>
  <done>Setup screen renders the full wizard flow with required final review and resume/restart prompt, and completion is independent from source connection count.</done>
</task>

<task type="auto">
  <name>Task 2: Wire onboarding provider into app startup and router redirect rules</name>
  <files>lib/main.dart, lib/app.dart, lib/router.dart</files>
  <action>Register onboarding provider during startup, loading draft state before first route resolution. Update router redirect helpers to keep setup gating tied to setup completion only (not source completion), maintain existing profile/adult route protections, and keep home entry behavior consistent after wizard completion. Ensure browse/discover default path continues to include all enabled sources with no source-type ordering bias.</action>
  <verify>flutter analyze lib/main.dart lib/app.dart lib/router.dart</verify>
  <done>Router and startup wiring are deterministic: incomplete setup redirects to onboarding, completed setup reaches discover, and source incompletion never blocks home entry.</done>
</task>

<task type="auto">
  <name>Task 3: Add onboarding flow and redirect regression tests for phase acceptance</name>
  <files>test/onboarding/setup_wizard_flow_test.dart, test/router/onboarding_redirect_test.dart</files>
  <action>Add tests covering end-to-end locked behaviors: checklist progression through linear steps, resume/restart branch behavior after interruption, no preselected library cards, adult privacy section rendering, mandatory review summary before finish, separate SFTP/FTP/WebDAV cards in source step, and routing that allows finishing without completed source setup. Include redirect tests ensuring no hard gate on source completion.</action>
  <verify>flutter test test/onboarding/setup_wizard_flow_test.dart && flutter test test/router/onboarding_redirect_test.dart</verify>
  <done>Regression tests pass and prove ONB-01/02/03/04 acceptance behaviors, including optional source completion and required final review gate.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` for setup/router/startup files and run onboarding/redirect tests for the full first-run flow.
</verification>

<success_criteria>
A first-time user can complete the new linear onboarding wizard with resumable progress and required final review while source setup remains optional and available for local/OneDrive/remote paths.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-source-setup/03-03-SUMMARY.md`
</output>
