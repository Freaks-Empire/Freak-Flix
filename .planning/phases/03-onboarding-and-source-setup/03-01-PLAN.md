---
phase: 03-onboarding-and-source-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/onboarding_draft.dart
  - lib/services/onboarding_draft_service.dart
  - lib/providers/onboarding_provider.dart
  - test/onboarding/onboarding_provider_test.dart
autonomous: true
requirements:
  - ONB-01
must_haves:
  truths:
    - User can resume interrupted onboarding at the last completed checklist state.
    - User can restart onboarding and clear draft progress in one action.
    - User can select library types with no preselected options.
    - Adult type is presented in a separate explicit privacy section.
    - User must complete a final review step before onboarding can be marked complete.
  artifacts:
    - lib/models/onboarding_draft.dart defines persisted draft state (step index, selected types, per-source statuses).
    - lib/services/onboarding_draft_service.dart persists non-secret onboarding draft checkpoints.
    - lib/providers/onboarding_provider.dart exposes wizard transitions, resume/restart behavior, and review gating.
    - test/onboarding/onboarding_provider_test.dart verifies draft transitions and required review completion.
  key_links:
    - onboarding provider reads/writes draft state through onboarding draft service.
    - onboarding provider maps selected library type cards to review-ready completion status.
    - onboarding completion gate requires review step flag before marking flow complete.
---

<objective>
Create a resumable onboarding state machine for first-run library-type setup with checklist progress and required final review.

Purpose: Implement ONB-01 with the locked UX decisions before screen-level wiring starts.
Output: New onboarding draft model/service/provider plus provider-level tests.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-source-setup/03-CONTEXT.md
@.planning/phases/03-onboarding-and-source-setup/03-RESEARCH.md
@lib/providers/settings_provider.dart
@lib/screens/setup_screen.dart
@lib/models/library_folder.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement persisted onboarding draft domain model and storage service</name>
  <files>lib/models/onboarding_draft.dart, lib/services/onboarding_draft_service.dart</files>
  <action>Create an `OnboardingDraft` model with explicit fields for `currentStep`, checklist completion flags, selected `LibraryType` values, per-source connection statuses (`not_started`, `in_progress`, `incomplete`, `connected`, `failed`), and final-review acknowledgement. Persist this draft in `SharedPreferences` as non-secret data (do not store credentials/tokens). Add load/save/clear APIs with defensive parsing defaults so missing keys remain safe and recoverable.</action>
  <verify>flutter analyze lib/models/onboarding_draft.dart lib/services/onboarding_draft_service.dart</verify>
  <done>Draft state can be loaded, updated, and cleared without crashing, and serialization preserves selected library types and source status checkpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Build onboarding provider state machine with resume/restart and review gate</name>
  <files>lib/providers/onboarding_provider.dart</files>
  <action>Create a `ChangeNotifier` onboarding provider that drives linear wizard transitions and checklist-style states. Add APIs for `resumeDraft()`, `restartFromBeginning()`, `goToNextStep()`, and `goToPreviousStep()` with strict step ordering. Enforce locked decisions: no preselected library types, adult type managed as a separate explicit privacy section, and onboarding completion blocked until final review is acknowledged (but not blocked by missing source connections). Include helper methods for card-based type toggles and review summary generation.</action>
  <verify>flutter analyze lib/providers/onboarding_provider.dart</verify>
  <done>Provider exposes deterministic step/checklist transitions, supports resume/restart behavior, and cannot mark onboarding complete until final review acknowledgement is true.</done>
</task>

<task type="auto">
  <name>Task 3: Add provider tests for resume/restart, no-default selection, and final review requirement</name>
  <files>test/onboarding/onboarding_provider_test.dart</files>
  <action>Add unit tests that cover: initial state has zero selected library types; restarting clears selected types and source statuses; resuming restores the exact step index/checklist; adult selection is represented as a separate privacy section flag; completion attempt fails before final review and succeeds after review acknowledgement. Keep tests isolated from network/service connectors.</action>
  <verify>flutter test test/onboarding/onboarding_provider_test.dart</verify>
  <done>All onboarding provider tests pass and explicitly prove ONB-01 locked behaviors for selection defaults, resume/restart, and required review.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on touched onboarding model/service/provider files and run `flutter test test/onboarding/onboarding_provider_test.dart`.
</verification>

<success_criteria>
Onboarding draft persistence and state transitions are deterministic, resumable, restartable, and enforce required final review before completion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-source-setup/03-01-SUMMARY.md`
</output>
