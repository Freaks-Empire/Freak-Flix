---
phase: 04-library-ingestion-and-indexing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - lib/services/incremental_scan_service.dart
  - lib/services/media_index_store.dart
  - lib/providers/library_provider.dart
  - test/services/incremental_scan_test.dart
autonomous: true
requirements:
  - LIB-04
  - LIB-05
  - PLAT-03
must_haves:
  truths:
    - Indexed items preserve unified local/remote source references.
    - Re-scan detects new, changed, and deleted files efficiently.
    - Large libraries remain responsive during scanning.
  artifacts:
    - lib/services/incremental_scan_service.dart provides delta detection.
    - lib/services/media_index_store.dart stores indexed items with metadata.
  key_links:
    - IncrementalScanService uses FilenameParserService from plan 04-02.
    - MediaIndexStore integrates with existing persistence layer.
---

<objective>
Implement incremental scanning with change detection and unified index storage.

Purpose: Enable efficient re-scans without full rebuilds, maintaining responsive UI on large libraries.
Output: Incremental scan service, media index store.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-library-ingestion-and-indexing/04-RESEARCH.md
@.planning/phases/04-library-ingestion-and-indexing/04-01-PLAN.md
@.planning/phases/04-library-ingestion-and-indexing/04-02-PLAN.md
@lib/models/media_item.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create media index store</name>
  <files>lib/services/media_index_store.dart</files>
  <action>
    Create MediaIndexStore that:
    - Stores indexed MediaItem with source reference (path, accountId, protocol)
    - Uses SQLite (sqflite) for local database
    - Schema: id, libraryId, title, year, type, filePath, accountId, protocol, 
      fileSize, modTime, hash, indexedAt
    - Supports queries by library, type, title search
    - Handles database migrations
    
    Reuses existing MediaItem model where possible.
  </action>
  <verify>flutter analyze lib/services/media_index_store.dart</verify>
  <done>MediaIndexStore persists indexed items with source references.</done>
</task>

<task type="auto">
  <name>Task 2: Create incremental scan service</name>
  <files>lib/services/incremental_scan_service.dart</files>
  <action>
    Create IncrementalScanService that:
    - Compares current scan results against stored index
    - Categorizes files: new, changed (size/modTime/hash), deleted
    - Returns delta summary: addedCount, updatedCount, removedCount
    - Supports full rescan option for re-indexing
    
    Uses file hash + size + modification time for change detection.
  </action>
  <verify>flutter analyze lib/services/incremental_scan_service.dart</verify>
  <done>IncrementalScanService correctly detects file changes.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate incremental scan with library provider</name>
  <files>lib/providers/library_provider.dart</files>
  <action>
    Extend LibraryProvider with:
    - scanLibrary(libraryId) using UnifiedScanService + FilenameParser
    - getIndexedItems(libraryId) from MediaIndexStore
    - rescanLibrary(libraryId) with incremental detection
    - Background scan support with progress callbacks
    
    Wire in ScanJobQueue from plan 04-01.
  </action>
  <verify>flutter analyze lib/providers/library_provider.dart</verify>
  <done>Users can scan and rescan libraries through provider API.</done>
</task>

<task type="auto">
  <name>Task 4: Add incremental scan tests</name>
  <files>test/services/incremental_scan_test.dart</files>
  <action>
    Add tests for:
    - Detects new files not in index
    - Detects files with changed size/modTime
    - Detects deleted files removed from source
    - Returns correct delta counts
    - Full rescan includes all files
  </action>
  <verify>flutter test test/services/incremental_scan_test.dart</verify>
  <done>Incremental scan has adequate test coverage.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on all modified files.
Run `flutter test` for incremental scan tests.
</verification>

<success_criteria>
Indexed items preserve source references for unified access.
Re-scan efficiently detects changes without full rebuild.
Large library scans remain responsive through incremental approach.
</success_criteria>

<output>
After completion, create `.planning/phases/04-library-ingestion-and-indexing/04-03-SUMMARY.md`
</output>
