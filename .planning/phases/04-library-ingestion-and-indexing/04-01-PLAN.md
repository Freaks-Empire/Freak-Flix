---
phase: 04-library-ingestion-and-indexing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/scan_orchestration_service.dart
  - lib/services/scan_job_queue.dart
  - lib/services/unified_scan_service.dart
  - test/services/scan_job_queue_test.dart
autonomous: true
requirements:
  - ONB-05
  - LIB-01
must_haves:
  truths:
    - User can start a scan and see real-time progress updates.
    - User can cancel an in-progress scan.
    - Scan works for both local folders and remote sources (OneDrive, SFTP, FTP, WebDAV).
  artifacts:
    - lib/services/unified_scan_service.dart provides scan entry point accepting LibraryFolder.
    - lib/services/scan_job_queue.dart manages concurrent scan jobs with progress callbacks.
    - lib/services/scan_orchestration_service.dart exposes progress to UI via provider.
  key_links:
    - ScanJobQueue consumes LibraryFolder from onboarding provider output.
    - UnifiedScanService uses existing RemoteStorageService for remote file listing.
---

<objective>
Build unified scan engine with progress tracking and job queue for local and remote sources.

Purpose: Foundation for all library ingestion - enables starting scans, monitoring progress, and cancellation.
Output: Scan orchestration service, job queue, unified scan entry point.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-library-ingestion-and-indexing/04-RESEARCH.md
@lib/services/scan_orchestration_service.dart
@lib/models/library_folder.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scan job queue with progress callbacks</name>
  <files>lib/services/scan_job_queue.dart</files>
  <action>
    Create ScanJobQueue service that:
    - Enqueues scan jobs with unique IDs
    - Reports progress via callback (files scanned, current file, percentage)
    - Supports cancellation via job ID
    - Uses isolates for heavy file processing on supported platforms
    - Persists job state for recovery on app restart
    
    Include: JobState enum (pending, running, completed, failed, cancelled), 
    ScanProgress class with scannedCount, totalCount, currentFile, percentage.
  </action>
  <verify>flutter analyze lib/services/scan_job_queue.dart</verify>
  <done>ScanJobQueue can enqueue jobs, report progress, and support cancellation.</done>
</task>

<task type="auto">
  <name>Task 2: Create unified scan service for local and remote sources</name>
  <files>lib/services/unified_scan_service.dart</files>
  <action>
    Create UnifiedScanService that:
    - Accepts LibraryFolder as input (local or remote)
    - Delegates to appropriate file enumerator (local IO, SFTP, FTP, WebDAV, OneDrive)
    - Returns Stream<ScanProgress> for real-time updates
    - Uses existing RemoteStorageService for protocol handling
    
    Support all LibraryFolder types: local, sftp:, ftp:, webdav:, OneDrive (via accountId)
  </action>
  <verify>flutter analyze lib/services/unified_scan_service.dart</verify>
  <done>UnifiedScanService can scan any supported source type and report progress.</done>
</task>

<task type="auto">
  <name>Task 3: Extend scan orchestration service for UI integration</name>
  <files>lib/services/scan_orchestration_service.dart</files>
  <action>
    Extend ScanOrchestrationService to:
    - Expose active job state to UI via provider (current job, progress, status)
    - Integrate with ScanJobQueue for job management
    - Provide startScan(LibraryFolder), cancelScan(jobId), getScanProgress() methods
    - Handle platform constraints (foreground service on Android, web limitations)
    
    Reuse existing notification infrastructure from ScanOrchestrationService.
  </action>
  <verify>flutter analyze lib/services/scan_orchestration_service.dart</verify>
  <done>Users can start scans from UI and see progress updates in real-time.</done>
</task>

<task type="auto">
  <name>Task 4: Add scan job queue tests</name>
  <files>test/services/scan_job_queue_test.dart</files>
  <action>
    Add tests covering:
    - Job enqueue returns unique ID
    - Progress callback receives updates
    - Cancellation stops running job
    - Concurrent jobs are queued properly
    - Job state persistence
  </action>
  <verify>flutter test test/services/scan_job_queue_test.dart</verify>
  <done>ScanJobQueue has adequate test coverage.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on all modified files.
Run `flutter test` for queue tests.
</verification>

<success_criteria>
User can start a scan from the app, see real-time progress, and cancel if needed.
Scan works for local folders, OneDrive, SFTP, FTP, and WebDAV sources.
</success_criteria>

<output>
After completion, create `.planning/phases/04-library-ingestion-and-indexing/04-01-SUMMARY.md`
</output>
