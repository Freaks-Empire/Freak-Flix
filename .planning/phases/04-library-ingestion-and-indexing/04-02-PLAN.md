---
phase: 04-library-ingestion-and-indexing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/library.dart
  - lib/services/library_service.dart
  - lib/services/filename_parser_service.dart
  - test/services/filename_parser_test.dart
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
must_haves:
  truths:
    - User can create, edit, and delete libraries with type/source associations.
    - Scanner correctly identifies movies from Title (Year) style filenames.
    - Scanner correctly identifies TV/Anime from SxxExx patterns.
    - Indexed items store source reference for later access.
  artifacts:
    - lib/models/library.dart provides Library model with metadata.
    - lib/services/library_service.dart handles CRUD operations.
    - lib/services/filename_parser_service.dart parses movie/TV/Anime patterns.
  key_links:
    - LibraryService integrates with existing LibraryProvider for state.
    - FilenameParser outputs MediaItem with source reference embedded.
---

<objective>
Implement library management and filename parsing for movie/TV/anime recognition.

Purpose: Enable users to manage multiple libraries and automatically detect media type from filenames.
Output: Library model, CRUD service, filename parser.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-library-ingestion-and-indexing/04-RESEARCH.md
@lib/models/library_folder.dart
@lib/providers/library_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Library model</name>
  <files>lib/models/library.dart</files>
  <action>
    Create Library model with:
    - id, name, path, type (LibraryType), accountId (for remote)
    - enabled: bool for active/inactive
    - lastScanAt: DateTime?
    - itemCount: int
    - scanState: LibraryScanState (idle, scanning, error)
    - source reference preservation for local and remote paths
    
    Include serialization (toJson/fromJson) for persistence.
  </action>
  <verify>flutter analyze lib/models/library.dart</verify>
  <done>Library model supports all required fields and serialization.</done>
</task>

<task type="auto">
  <name>Task 2: Create library service for CRUD operations</name>
  <files>lib/services/library_service.dart</files>
  <action>
    Create LibraryService that:
    - addLibrary(Library), updateLibrary(Library), deleteLibrary(id)
    - getLibraries(), getLibraryById(id), getLibrariesByType(type)
    - updateScanState for progress tracking
    - Persists to SharedPreferences/JSON
    
    Integrates with existing LibraryFolder model from onboarding phase.
  </action>
  <verify>flutter analyze lib/services/library_service.dart</verify>
  <done>Users can manage libraries through service API.</done>
</task>

<task type="auto">
  <name>Task 3: Create filename parser for movie patterns</name>
  <files>lib/services/filename_parser_service.dart</files>
  <action>
    Create FilenameParserService with movie parsing:
    - Recognizes: Title (Year), Title.Year, Title.Year.1080p, etc.
    - Extracts: title, year, resolution, quality, group
    - Handles common extensions: mp4, mkv, avi, mov, wmv
    
    Uses regex patterns with fallback for unknown formats.
  </action>
  <verify>flutter analyze lib/services/filename_parser_service.dart</verify>
  <done>Movie filenames are parsed into structured title/year data.</done>
</task>

<task type="auto">
  <name>Task 4: Create filename parser for TV/Anime patterns</name>
  <files>lib/services/filename_parser_service.dart</files>
  <action>
    Extend FilenameParserService with TV/Anime parsing:
    - S01E01, 1x01, Season X Episode YY formats
    - Anime: Episode [resolution], Anime - 001 patterns
    - Extracts: title, season, episode, episodeTitle
    
    Priority: Try TV/Anime patterns first, fall back to movie.
  </action>
  <verify>flutter analyze lib/services/filename_parser_service.dart</verify>
  <done>TV and Anime filenames are parsed into structured data.</done>
</task>

<task type="auto">
  <name>Task 5: Add filename parser tests</name>
  <files>test/services/filename_parser_test.dart</files>
  <action>
    Add comprehensive tests for:
    - Movie: Title (2024), Title.2024.1080p, Title (2024) PROPER
    - TV: S01E01, 1x01, Season 1 Episode 01
    - Anime: Anime - 001, Anime S01E01
    - Edge cases: Multiple dots, underscore variations, 4-digit year
  </action>
  <verify>flutter test test/services/filename_parser_test.dart</verify>
  <done>Filename parser has adequate test coverage.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on all modified files.
Run `flutter test` for filename parser tests.
</verification>

<success_criteria>
Users can create, edit, and delete libraries.
Scanner correctly identifies media type from filename patterns.
Indexed items preserve source reference for access.
</success_criteria>

<output>
After completion, create `.planning/phases/04-library-ingestion-and-indexing/04-02-SUMMARY.md`
</output>
