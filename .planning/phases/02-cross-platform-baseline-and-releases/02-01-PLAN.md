---
phase: 02-cross-platform-baseline-and-releases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/providers/library_provider.dart
  - lib/services/scan_orchestration_service.dart
  - lib/widgets/settings/settings_sync_section.dart
  - lib/widgets/trailer_player.dart
  - lib/utils/platform/platform.dart
  - test/platform/platform_guard_test.dart
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - Core journey entry points run without platform-guard runtime exceptions on Windows, Android, and Web.
    - Platform-limited capabilities fail gracefully with explicit "not available yet" messaging instead of crashes or dead ends.
    - Settings and navigation semantics stay consistent across platforms even when interaction models differ.
  artifacts:
    - path: lib/main.dart
      provides: Web-safe startup logic that avoids unsupported platform calls before app initialization.
    - path: lib/providers/library_provider.dart
      provides: Runtime-safe platform branching for cross-platform library workflows.
    - path: lib/services/scan_orchestration_service.dart
      provides: Guarded orchestration behavior that preserves reliability across desktop, mobile, and web runtimes.
    - path: test/platform/platform_guard_test.dart
      provides: Regression coverage for platform guard behavior and unsupported-path handling.
  key_links:
    - from: lib/main.dart
      to: lib/utils/platform/platform.dart
      via: startup platform checks route through shared abstraction boundary
      pattern: Platform\.
    - from: lib/widgets/settings/settings_sync_section.dart
      to: lib/services/scan_orchestration_service.dart
      via: unsupported operations resolve to explicit user-facing states
      pattern: not available|unsupported|Platform\.
---

<objective>
Harden platform guardrails in shared runtime paths so mandatory journeys start and route safely on Windows, Android, and Web.

Purpose: PLAT-01 depends on predictable runtime behavior; unsupported platform calls in shared code create crash/dead-end risk that must be removed first.
Output: Unified platform-safe branching in startup/orchestration/settings paths plus regression tests for guard behavior.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-CONTEXT.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-RESEARCH.md
@lib/main.dart
@lib/providers/library_provider.dart
@lib/services/scan_orchestration_service.dart
@lib/widgets/settings/settings_sync_section.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate unsafe shared-platform entry checks and standardize guard boundary</name>
  <files>lib/main.dart, lib/providers/library_provider.dart, lib/utils/platform/platform.dart</files>
  <action>Replace direct `dart:io Platform` usage in shared runtime entry points with web-safe branching (`kIsWeb` plus shared platform abstraction). Ensure startup and provider code never evaluates unsupported platform APIs on web and preserves existing behavior on Windows/Android.</action>
  <verify>flutter analyze lib/main.dart lib/providers/library_provider.dart lib/utils/platform/platform.dart</verify>
  <done>Startup and provider platform checks are deterministic and web-safe without changing core journey semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit unsupported-state handling for platform-limited operations</name>
  <files>lib/services/scan_orchestration_service.dart, lib/widgets/settings/settings_sync_section.dart, lib/widgets/trailer_player.dart</files>
  <action>Harden scan/sync/trailer code paths so unsupported platform operations return explicit not-available states with actionable guidance. Keep route/settings semantics intact and avoid silent feature disappearance.</action>
  <verify>flutter analyze lib/services/scan_orchestration_service.dart lib/widgets/settings/settings_sync_section.dart lib/widgets/trailer_player.dart</verify>
  <done>Unsupported operations surface clear, recoverable UX and do not produce runtime exceptions in scoped journeys.</done>
</task>

<task type="auto">
  <name>Task 3: Add platform guard regression tests for startup and unsupported branches</name>
  <files>test/platform/platform_guard_test.dart</files>
  <action>Create regression tests covering web-safe startup checks, platform branch correctness, and unsupported-state returns for platform-limited operations touched in this plan.</action>
  <verify>flutter test test/platform/platform_guard_test.dart</verify>
  <done>Automated tests fail on guard regressions and pass with hardened platform safety behavior.</done>
</task>

</tasks>

<verification>
Run `flutter test test/platform/platform_guard_test.dart` and targeted `flutter analyze` on touched startup/provider/platform files.
</verification>

<success_criteria>
- Shared startup/provider paths are safe across Windows, Android, and Web.
- Platform-limited operations fail gracefully with explicit messaging.
- Guard behavior is covered by repeatable regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-platform-baseline-and-releases/02-01-SUMMARY.md`
</output>
