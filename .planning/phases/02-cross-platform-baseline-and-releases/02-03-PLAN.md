---
phase: 02-cross-platform-baseline-and-releases
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/release/preflight.ps1
  - scripts/release/build_all_release.ps1
  - scripts/release/build_web_release.ps1
  - scripts/release/build_windows_release.ps1
  - scripts/release/build_android_release.ps1
  - android/gradle.properties
  - pubspec.yaml
  - .planning/phases/02-cross-platform-baseline-and-releases/02-release-artifacts.md
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - Windows, Android, and Web release builds can be executed through repeatable documented commands.
    - Release pipeline output includes artifact existence evidence and warning/blocker classification.
    - Android release build path is stable enough for repeated runs or has explicit scripted mitigation and logging.
  artifacts:
    - path: scripts/release/preflight.ps1
      provides: Toolchain and environment readiness checks before release builds.
    - path: scripts/release/build_all_release.ps1
      provides: One-command orchestrated release build run for web/windows/android with status capture.
    - path: android/gradle.properties
      provides: Android build stability tuning for repeatable release execution.
    - path: .planning/phases/02-cross-platform-baseline-and-releases/02-release-artifacts.md
      provides: Artifact ledger with output paths, timestamps, and warning/blocker notes.
  key_links:
    - from: scripts/release/build_all_release.ps1
      to: scripts/release/preflight.ps1
      via: orchestration runs preflight gate before platform builds
      pattern: preflight
    - from: scripts/release/build_android_release.ps1
      to: android/gradle.properties
      via: Android release command uses tuned gradle settings and clean strategy
      pattern: gradle|kotlin|daemon|clean
    - from: scripts/release/build_all_release.ps1
      to: .planning/phases/02-cross-platform-baseline-and-releases/02-release-artifacts.md
      via: build outcomes and artifact paths are persisted in release ledger
      pattern: build\\\\|app-release.apk|freakflix.exe|build\\\\web
---

<objective>
Establish reproducible release-build execution for Windows, Android, and Web with concrete artifact evidence.

Purpose: PLAT-02 requires deterministic release output readiness; current local success is noisy and not yet codified into repeatable build discipline.
Output: Preflight + per-platform build scripts, Android release stability tuning, and release artifact ledger with warning/blocker classification.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-CONTEXT.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-RESEARCH.md
@.planning/phases/02-cross-platform-baseline-and-releases/02-01-SUMMARY.md
@pubspec.yaml
@android/gradle.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release preflight and unified build orchestration scripts</name>
  <files>scripts/release/preflight.ps1, scripts/release/build_all_release.ps1, scripts/release/build_web_release.ps1, scripts/release/build_windows_release.ps1, scripts/release/build_android_release.ps1</files>
  <action>Create deterministic PowerShell scripts that run toolchain preflight, invoke platform release builds, capture exit codes, and emit concise status output suitable for local and CI use.</action>
  <verify>pwsh ./scripts/release/preflight.ps1 && pwsh ./scripts/release/build_all_release.ps1 -NoPublish</verify>
  <done>Release build orchestration is runnable via one top-level command with reproducible per-platform execution paths.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize Android release path and remove avoidable build warning drift</name>
  <files>android/gradle.properties, pubspec.yaml, scripts/release/build_android_release.ps1</files>
  <action>Apply Android build stability mitigations (clean strategy, gradle/kotlin daemon settings, retry-safe script behavior) and address preventable build warning noise such as missing icon font package references so release output classification is clearer.</action>
  <verify>flutter pub get && pwsh ./scripts/release/build_android_release.ps1</verify>
  <done>Android release path is repeatable with actionable warning output and no avoidable configuration drift.</done>
</task>

<task type="auto">
  <name>Task 3: Generate release artifact ledger for PLAT-02 evidence</name>
  <files>.planning/phases/02-cross-platform-baseline-and-releases/02-release-artifacts.md</files>
  <action>Record release run metadata per platform (command, artifact path, timestamp, result, warning/blocker classification) and include direct links to produced outputs used for phase gate review.</action>
  <verify>rg -n \"Windows|Android|Web|artifact|status|warning|blocker\" .planning/phases/02-cross-platform-baseline-and-releases/02-release-artifacts.md</verify>
  <done>Phase has a reproducible release evidence ledger proving PLAT-02 build-output readiness or clearly documenting blockers.</done>
</task>

</tasks>

<verification>
Run scripted release preflight and platform release builds; confirm artifact ledger reflects actual command outcomes and output files.
</verification>

<success_criteria>
- Release builds are scriptable and repeatable for Windows, Android, and Web.
- Android release path is stabilized with explicit mitigation for prior noisy failures.
- Artifact ledger provides deterministic PLAT-02 evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-platform-baseline-and-releases/02-03-SUMMARY.md`
</output>
