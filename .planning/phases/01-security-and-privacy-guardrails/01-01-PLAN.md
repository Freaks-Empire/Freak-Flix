---
phase: 01-security-and-privacy-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/security_validation_result.dart
  - lib/utils/security_policies.dart
  - lib/utils/input_validation.dart
  - lib/widgets/settings/remote_connection_dialog.dart
  - test/helpers/security_test_helpers.dart
  - test/security/command_injection_test.dart
  - test/security/ssrf_test.dart
autonomous: true
requirements:
  - SEC-01
  - SEC-03
must_haves:
  truths:
    - User-entered connector values cannot be used to pass command-injection payloads.
    - Unsafe remote targets (loopback/private/internal/metadata endpoints) are blocked before connection tests or saves.
    - Validation feedback is calm and specific, with clear reason and concrete fix examples.
    - Formatting-only issues are warnings, while exploit-risk input is hard-blocked.
    - Repeated blocked submits trigger expanded guidance with a help/docs path.
  artifacts:
    - path: lib/utils/security_validation_result.dart
      provides: Typed validation result model (ok/warning/blocking + reason + fix + safe default).
    - path: lib/utils/security_policies.dart
      provides: Centralized SSRF and command-injection policy constants.
    - path: lib/utils/input_validation.dart
      provides: Canonicalized submit-time validation and lightweight typing-time checks.
    - path: lib/widgets/settings/remote_connection_dialog.dart
      provides: Hybrid validation UX with strict submit enforcement and escalation behavior.
    - path: test/security/command_injection_test.dart
      provides: Regression coverage for blocked command-injection payloads and allowed safe input.
    - path: test/security/ssrf_test.dart
      provides: Regression coverage for blocked SSRF targets and allowed public hosts.
  key_links:
    - from: lib/widgets/settings/remote_connection_dialog.dart
      to: lib/utils/input_validation.dart
      via: onChanged warnings + submit-time blocking validation pipeline
      pattern: InputValidation\.
    - from: lib/utils/input_validation.dart
      to: lib/utils/security_policies.dart
      via: shared host/IP/metadata denylist policy checks
      pattern: SecurityPolicies
    - from: test/helpers/security_test_helpers.dart
      to: test/security/command_injection_test.dart
      via: corrected blocked-vs-allowed assertion contract
      pattern: expectSecurity(Blocked|Allowed)
---

<objective>
Replace string-only validation with a shared typed security validation pipeline and enforce it in remote connector UX.

Purpose: SEC-01 and SEC-03 are foundational for every later storage/metadata phase; this plan creates one reusable safety contract and removes policy drift.
Output: Unified validation models/policies, wired remote dialog behavior aligned to locked UX decisions, and re-baselined command-injection/SSRF security tests.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-privacy-guardrails/01-CONTEXT.md
@.planning/phases/01-security-and-privacy-guardrails/01-RESEARCH.md
@lib/utils/input_validation.dart
@lib/widgets/settings/remote_connection_dialog.dart
@test/helpers/security_test_helpers.dart
@test/security/command_injection_test.dart
@test/security/ssrf_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce typed security validation contract and centralized policies</name>
  <files>lib/utils/security_validation_result.dart, lib/utils/security_policies.dart, lib/utils/input_validation.dart</files>
  <action>Create `SecurityValidationResult` and severity enums to encode `ok`, `warning`, and `blocking` outcomes with required `reason` and `fixExample` fields plus optional safe-default payload. Refactor `InputValidation` so strict submit-time validators canonicalize user input (decode/normalize before checks), hard-block exploit-risk payloads (command injection + SSRF target classes), and keep warning-only outcomes to formatting/non-dangerous issues per locked context. Do not keep regex-only bypass-prone logic as the source of truth.</action>
  <verify>flutter analyze lib/utils/security_validation_result.dart lib/utils/security_policies.dart lib/utils/input_validation.dart</verify>
  <done>`InputValidation` exposes typed strict validation methods for connector fields, and policy sources are centralized with no duplicated hard-coded risk tables.</done>
</task>

<task type="auto">
  <name>Task 2: Apply hybrid validation UX with escalation and equal-weight recovery actions</name>
  <files>lib/widgets/settings/remote_connection_dialog.dart</files>
  <action>Update remote connector form to use lightweight checks while typing and strict typed validation on submit. All blocking outcomes must show calm/user-first copy with exact reason + concrete fix example. Add one-tap safe-default actions (where applicable) and keep safe-default/manual-edit actions visually equal weight. Implement repeated blocked-submit escalation threshold at 3 attempts per field per dialog session (discretion call), showing expanded guidance and a docs/help link without changing hard-block behavior.</action>
  <verify>flutter analyze lib/widgets/settings/remote_connection_dialog.dart</verify>
  <done>Dialog behavior matches locked UX decisions exactly: hybrid checks, warning vs blocking split, reason+fix messaging, equal-weight recovery actions, and escalation after repeated blocked attempts.</done>
</task>

<task type="auto">
  <name>Task 3: Correct security test contracts and lock in SEC-01/SEC-03 regressions</name>
  <files>test/helpers/security_test_helpers.dart, test/security/command_injection_test.dart, test/security/ssrf_test.dart</files>
  <action>Fix helper semantics so blocked payload assertions align with the validation contract and safe inputs are allowed. Rebaseline command-injection and SSRF tests against typed validation outcomes, including private/link-local/loopback/internal host patterns and encoded bypass attempts. Ensure tests target strict submit-time validation methods and avoid depending on transient warning copy text.</action>
  <verify>flutter test test/security/command_injection_test.dart && flutter test test/security/ssrf_test.dart</verify>
  <done>Security tests consistently pass with assertion semantics matching real block/allow behavior, and critical SEC-01 + SEC-03 attacks are covered by repeatable regression tests.</done>
</task>

</tasks>

<verification>
Run `flutter test test/security/command_injection_test.dart` and `flutter test test/security/ssrf_test.dart`, then run `flutter analyze` on touched validation/UI files; all pass with no new security warnings.
</verification>

<success_criteria>
- Connector submit paths block exploit-risk input with typed blocking results.
- Warning-only behavior is limited to formatting/non-dangerous issues.
- Users see reason + fix messaging and escalation guidance after repeated blocked submits.
- SSRF and command-injection regression tests are green and aligned to true security semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-privacy-guardrails/01-01-SUMMARY.md`
</output>
