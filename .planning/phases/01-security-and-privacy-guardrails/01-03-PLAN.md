---
phase: 01-security-and-privacy-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/secure_key_service.dart
  - lib/providers/settings_provider.dart
  - lib/models/stash_endpoint.dart
  - lib/widgets/settings/settings_metadata_section.dart
  - lib/widgets/settings/settings_advanced_section.dart
  - test/security/secret_storage_test.dart
autonomous: true
requirements:
  - SEC-04
must_haves:
  truths:
    - API keys and endpoint credentials are persisted via secure storage, not plain settings JSON.
    - Existing users are migrated without losing configured provider access.
    - Exported settings/backup state does not include secret plaintext values.
    - Secret-handling UX keeps editing and testing flows functional while secrets remain masked/redacted.
  artifacts:
    - path: lib/services/secure_key_service.dart
      provides: Platform-secure secret storage boundary using flutter_secure_storage.
    - path: lib/providers/settings_provider.dart
      provides: Non-secret settings persistence with secret indirection/migration logic.
    - path: lib/models/stash_endpoint.dart
      provides: Endpoint model that no longer serializes secret key material into general JSON state.
    - path: test/security/secret_storage_test.dart
      provides: Regression tests ensuring secrets are excluded from serialized/exported settings artifacts.
  key_links:
    - from: lib/providers/settings_provider.dart
      to: lib/services/secure_key_service.dart
      via: load/save migration path from legacy plaintext secrets to secure storage
      pattern: SecureKeyService
    - from: lib/widgets/settings/settings_metadata_section.dart
      to: lib/providers/settings_provider.dart
      via: API key update flow writes through secure storage boundary
      pattern: setTmdbApiKey
    - from: lib/widgets/settings/settings_advanced_section.dart
      to: lib/providers/settings_provider.dart
      via: endpoint create/update path stores endpoint key securely instead of JSON field
      pattern: addStashEndpoint|updateStashEndpoint
---

<objective>
Move app/provider credentials to secure storage and eliminate plaintext secret persistence in settings artifacts.

Purpose: SEC-04 is a trust baseline for connector and metadata integrations; this plan removes high-risk secret leakage from current settings storage.
Output: Secure secret boundary, legacy secret migration path, settings serialization split, and tests proving secrets are not exported/committed via app artifacts.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-privacy-guardrails/01-RESEARCH.md
@lib/services/secure_key_service.dart
@lib/providers/settings_provider.dart
@lib/models/stash_endpoint.dart
@lib/widgets/settings/settings_metadata_section.dart
@lib/widgets/settings/settings_advanced_section.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework secure key boundary to platform-backed storage only</name>
  <files>lib/services/secure_key_service.dart</files>
  <action>Refactor `SecureKeyService` to rely on `flutter_secure_storage` directly for secret-at-rest handling and remove custom XOR/DIY encryption layers. Keep deterministic key namespaces for TMDB and per-Stash-endpoint secrets, add explicit read/write/delete APIs per secret type, and include migration helpers for legacy plaintext fields discovered during settings load.</action>
  <verify>flutter analyze lib/services/secure_key_service.dart</verify>
  <done>Secure key service provides clear platform-secure APIs without custom crypto logic and supports migration from legacy secret locations.</done>
</task>

<task type="auto">
  <name>Task 2: Split settings persistence into non-secret state plus secure secret references</name>
  <files>lib/providers/settings_provider.dart, lib/models/stash_endpoint.dart</files>
  <action>Update settings model serialization to persist only non-secret metadata in JSON (flags, URLs, endpoint IDs/names, status), while reading/writing actual key material through `SecureKeyService`. Add one-time migration in `SettingsProvider.load()` for existing `tmdbApiKey`/`stash apiKey` plaintext values and scrub them from saved JSON afterward. Ensure export/import paths remain functional without reintroducing plaintext secrets.</action>
  <verify>flutter analyze lib/providers/settings_provider.dart lib/models/stash_endpoint.dart</verify>
  <done>Settings JSON no longer includes API secrets, migration path preserves existing user configuration, and runtime access still retrieves secrets correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Update settings UI wiring and add secret-leak regression tests</name>
  <files>lib/widgets/settings/settings_metadata_section.dart, lib/widgets/settings/settings_advanced_section.dart, test/security/secret_storage_test.dart</files>
  <action>Adjust metadata/advanced settings UI handlers so key updates route through provider methods backed by secure storage and preserve current usability (masking, edit, test connection). Add `secret_storage_test.dart` to assert serialized settings/export maps do not contain TMDB/Stash secret values and migrated data paths scrub legacy secret fields.</action>
  <verify>flutter test test/security/secret_storage_test.dart</verify>
  <done>UI key flows remain operational while secrets stay outside plain persisted/exported artifacts, validated by dedicated security tests.</done>
</task>

</tasks>

<verification>
Run `flutter test test/security/secret_storage_test.dart` and `flutter analyze` for touched settings/security files; confirm no plaintext API keys appear in serialized settings fixture outputs.
</verification>

<success_criteria>
- TMDB and Stash keys are persisted via secure storage APIs.
- Legacy plaintext secrets are migrated and removed from settings JSON.
- Exported settings artifacts contain metadata only and no secret plaintext.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-privacy-guardrails/01-03-SUMMARY.md`
</output>
