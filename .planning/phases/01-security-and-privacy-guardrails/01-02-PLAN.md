---
phase: 01-security-and-privacy-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/path_guard.dart
  - lib/services/persistence_service.dart
  - lib/services/data_backup_service.dart
  - test/security/directory_traversal_test.dart
  - test/security/path_guard_test.dart
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - Local backup/import/export paths cannot escape approved local roots through traversal tricks.
    - Encoded, mixed-separator, and normalized traversal payloads are blocked consistently.
    - Legitimate in-root paths continue to work across Windows and POSIX path forms.
  artifacts:
    - path: lib/utils/path_guard.dart
      provides: Canonical path containment helpers for local filesystem operations.
    - path: lib/services/persistence_service.dart
      provides: Root-constrained file read/write/delete behavior for app data files.
    - path: lib/services/data_backup_service.dart
      provides: Traversal-safe backup import/export entry-point validation.
    - path: test/security/path_guard_test.dart
      provides: Unit coverage for canonicalization and in-root containment logic.
    - path: test/security/directory_traversal_test.dart
      provides: Security regression coverage for traversal payload families.
  key_links:
    - from: lib/services/data_backup_service.dart
      to: lib/utils/path_guard.dart
      via: validate and canonicalize user-selected backup file paths before IO
      pattern: PathGuard
    - from: lib/services/persistence_service.dart
      to: lib/utils/path_guard.dart
      via: constrain all file operations to application support directory
      pattern: PathGuard
---

<objective>
Add canonical path-containment enforcement to local filesystem entry points and harden traversal defenses.

Purpose: SEC-02 requires real root containment, not string pattern checks; this plan enforces containment where IO actually occurs.
Output: Shared path guard utility, service-level containment checks, and traversal-focused tests that prove encoded/normalized bypasses are rejected.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-privacy-guardrails/01-RESEARCH.md
@lib/services/persistence_service.dart
@lib/services/data_backup_service.dart
@test/security/directory_traversal_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical path containment utility for local IO</name>
  <files>lib/utils/path_guard.dart</files>
  <action>Create a `PathGuard` utility that normalizes/decodes candidate paths, resolves them against an allowed root, and rejects paths outside root containment (including encoded traversal and separator tricks). Base behavior on `path` package canonicalization semantics; do not rely on substring `../` checks.</action>
  <verify>flutter analyze lib/utils/path_guard.dart</verify>
  <done>`PathGuard` provides deterministic allow/deny APIs for local file paths and supports both Windows and POSIX path semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce containment in persistence and backup service entry points</name>
  <files>lib/services/persistence_service.dart, lib/services/data_backup_service.dart</files>
  <action>Use `PathGuard` in persistence and backup import/export flows so all local file operations are constrained to approved roots (`ApplicationSupportDirectory` for app-managed files; user-selected backup roots for backup operations). Return actionable errors when blocked and avoid writing/reading files before validation completes.</action>
  <verify>flutter analyze lib/services/persistence_service.dart lib/services/data_backup_service.dart</verify>
  <done>All local filesystem operations in touched services perform canonical root containment checks before IO, and traversal attempts fail closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add traversal regression tests at utility and security-suite level</name>
  <files>test/security/path_guard_test.dart, test/security/directory_traversal_test.dart</files>
  <action>Add focused tests for `PathGuard` containment behavior and expand directory traversal security tests to include encoded, mixed-separator, and normalization-based bypasses against service-level guard behavior. Keep legitimate in-root path coverage to prevent false positives.</action>
  <verify>flutter test test/security/path_guard_test.dart && flutter test test/security/directory_traversal_test.dart</verify>
  <done>Traversal tests are green, bypass payloads are blocked, and valid paths inside allowed roots remain accepted.</done>
</task>

</tasks>

<verification>
Run `flutter test test/security/path_guard_test.dart`, `flutter test test/security/directory_traversal_test.dart`, and `flutter analyze` on changed utility/service files.
</verification>

<success_criteria>
- Service-level filesystem entry points reject traversal attempts after canonicalization.
- Encoded/normalized bypass patterns cannot escape allowed roots.
- Legitimate in-root paths still function on supported desktop/mobile path semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-privacy-guardrails/01-02-SUMMARY.md`
</output>
