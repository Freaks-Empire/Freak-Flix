---
phase: 01-security-and-privacy-guardrails
plan: 04
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - lib/providers/settings_provider.dart
  - lib/screens/setup_screen.dart
  - lib/widgets/settings/settings_advanced_section.dart
  - lib/router.dart
  - lib/widgets/navigation_dock.dart
  - test/security/adult_privacy_test.dart
autonomous: true
requirements:
  - SEC-05
must_haves:
  truths:
    - Adult library visibility is off by default for new and migrated users.
    - Adult content appears only after explicit user opt-in from setup/settings.
    - Navigation and routing prevent access to `/adult` when opt-in is disabled.
    - Disabling adult mode immediately hides adult navigation surfaces again.
  artifacts:
    - path: lib/providers/settings_provider.dart
      provides: Default-off adult preference and import/migration guardrails.
    - path: lib/screens/setup_screen.dart
      provides: Explicit opt-in control/copy for adult library visibility.
    - path: lib/widgets/settings/settings_advanced_section.dart
      provides: Explicit adult mode toggle copy/behavior aligned to privacy-first default.
    - path: lib/router.dart
      provides: Redirect gating that blocks `/adult` access when opt-in is false.
    - path: lib/widgets/navigation_dock.dart
      provides: Conditional adult nav item visibility tied to explicit opt-in state.
    - path: test/security/adult_privacy_test.dart
      provides: Regression tests for default-off, route gating, and nav visibility.
  key_links:
    - from: lib/providers/settings_provider.dart
      to: lib/router.dart
      via: `enableAdultContent` drives redirect behavior for `/adult`
      pattern: enableAdultContent
    - from: lib/providers/settings_provider.dart
      to: lib/widgets/navigation_dock.dart
      via: conditional rendering of Adult tab
      pattern: settings.enableAdultContent
---

<objective>
Enforce privacy-first adult-content behavior so adult surfaces remain hidden unless users explicitly opt in.

Purpose: SEC-05 is a hard trust/privacy contract and must remain deterministic across setup, settings, navigation, and routing.
Output: Explicit opt-in UX, default-off persistence guarantees, route/nav gating hardening, and security tests covering visibility and access behavior.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-privacy-guardrails/01-CONTEXT.md
@lib/providers/settings_provider.dart
@lib/screens/setup_screen.dart
@lib/widgets/settings/settings_advanced_section.dart
@lib/router.dart
@lib/widgets/navigation_dock.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden explicit opt-in flow and default-off persistence for adult mode</name>
  <files>lib/providers/settings_provider.dart, lib/screens/setup_screen.dart, lib/widgets/settings/settings_advanced_section.dart</files>
  <action>Ensure `enableAdultContent` defaults to false on first run, remains false through migrations/import unless explicitly enabled, and is presented as an explicit opt-in choice (not preselected) in setup/settings with privacy-first copy. Keep toggle behavior deterministic when users disable it again.</action>
  <verify>flutter analyze lib/providers/settings_provider.dart lib/screens/setup_screen.dart lib/widgets/settings/settings_advanced_section.dart</verify>
  <done>Adult mode cannot become enabled implicitly via defaults, migration artifacts, or passive setup completion; user intent is required.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce route and navigation gating for adult content surfaces</name>
  <files>lib/router.dart, lib/widgets/navigation_dock.dart</files>
  <action>Keep `/adult` route redirect protection strict and central in router logic, and ensure navigation dock shows Adult tab only when `enableAdultContent` is true. Verify disabled state immediately removes direct route access and nav affordance without stale UI state.</action>
  <verify>flutter analyze lib/router.dart lib/widgets/navigation_dock.dart</verify>
  <done>With adult opt-in off, users cannot navigate to adult screen by URL or UI tab; with opt-in on, adult surfaces become available as expected.</done>
</task>

<task type="auto">
  <name>Task 3: Add SEC-05 regression tests for default privacy behavior</name>
  <files>test/security/adult_privacy_test.dart</files>
  <action>Create tests covering default-off settings state, explicit toggle transitions, router redirect outcomes for `/adult`, and conditional Adult tab visibility behavior. Include regression case verifying turning adult mode off after enabling restores hidden/gated state.</action>
  <verify>flutter test test/security/adult_privacy_test.dart</verify>
  <done>Automated tests prove adult library remains hidden by default and only appears after explicit user opt-in.</done>
</task>

</tasks>

<verification>
Run `flutter test test/security/adult_privacy_test.dart` and targeted `flutter analyze` for touched provider/setup/router/navigation files.
</verification>

<success_criteria>
- Adult visibility defaults to off across fresh and migrated settings.
- Adult route/tab are inaccessible when opt-in is disabled.
- Explicit opt-in reliably enables adult surfaces and opt-out hides them again.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-privacy-guardrails/01-04-SUMMARY.md`
</output>
