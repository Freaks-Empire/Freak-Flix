name: Flutter Windows CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
      GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
      GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
      TRAKT_CLIENT_ID: ${{ secrets.TRAKT_CLIENT_ID }}
      OMDB_API_KEY: ${{ secrets.OMDB_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows (release)
        run: >
          flutter build windows --release
          --dart-define=TMDB_API_KEY=${{ env.TMDB_API_KEY }}
          --dart-define=GRAPH_CLIENT_ID=${{ env.GRAPH_CLIENT_ID }}
          --dart-define=GRAPH_TENANT_ID=${{ env.GRAPH_TENANT_ID }}
          --dart-define=TRAKT_CLIENT_ID=${{ env.TRAKT_CLIENT_ID }}
          --dart-define=OMDB_API_KEY=${{ env.OMDB_API_KEY }}
